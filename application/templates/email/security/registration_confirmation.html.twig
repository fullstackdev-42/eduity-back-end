{%  extends 'email/base.html.twig' %}

{%  block body %}
    <h3>Welcome {{  user.firstName ? user.firstName : 'User' }},<h3>
    <p>
        <a href="{{ url('user_register_confirm', {'id': user.Uuid, 'code': user.emailConfirmationCode}) }}">Click here to confirm email.</a>
    </p>
{%  endblock %}